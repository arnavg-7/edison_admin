"use client";

import { ColumnsView } from "@/components/school-setup/ColumnsView";

/** Schools, grades and batches side by side, so the whole path stays visible. */
export default function SchoolSetupColumnsPage() {
  return <ColumnsView />;
}
